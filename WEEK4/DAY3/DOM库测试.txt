1、getElementsByClassName的兼容处理
  /*
   * 1)首先判断是否兼容,兼容的话使用内置的getElementsByClassName方法即可，注意这样返回的是类数组，我们需要把其转换为数组，只有这样才能和不兼容处理返回的结果是一致的
   * 2)不兼容处理
   *   ->先获取当前指定上下文中所有的元素标签
   *   ->把传递进来的样式类名('  xxx    xxx ')拆分成样式类名数组(['xxx','xxx'])
   *   ->循环所有的元素标签，获取它的样式类名，和我们传递进来的样式类名集合进行一一匹配，使用假设法，假设当前循环的这个元素是我们想要的，那么在一一和传递样式进行比较的时候，只要有一个不符合，假设就错误，就不是我们想要的...
   *   ->把假设成功的元素放在一个容器中，最后这个容器就是我们想要的结果
  */

2、编写一个CSS方法实现对元素样式的（单一增加、批量增加、获取）的操作
  getCss
  setCss
  setGroupCss
  三者组合体

3、编写一个children的方法实现对children这个节点属性的兼容处理
  /*
   * ->先获取当前元素所有的子节点
   * ->在所有子节点中找到所有的元素子节点
   * ->如果传递了标签名，在所有的元素子节点中筛选出符合对应标签名的；如果没有传递标签名，则这一步筛选可以省略
  */

4、编写一个addClass方法，实现给当前元素增加样式类名的功能
  /*
   * ->如果当前元素已经存在此样式类名，将不再增加
  */